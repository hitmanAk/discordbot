import { REST,Routes } from "discord.js";

const commands = [
    {
        name:'hey',
        descriptions:'Replies with hey'
    }
]

const rest = new REST({})


(async()=>{
    try{

    }
    catch (error){
        console.log(error)
    }
})